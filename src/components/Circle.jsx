import React from 'react';
import "../index.css"


const Circle = (props) => {
    return (
      
        <div className="circle" onClick={props.click}>   
           
        </div>
    );
};

export default Circle;